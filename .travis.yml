language: node_js

node_js:
  - "5"

addons:
  firefox: "44.0"

branches:
  only:
    - master

cache:
  directories:
    - node_modules
    - bower_components

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
