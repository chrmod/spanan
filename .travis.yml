language: node_js

node_js:
  - "5"

addons:
  firefox: "44.0"

before_install:
  - npm install -g bower
  - npm install -g broccoli-cli
  - bower install

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
